export function App() {
  return (
    <main className="shell">
      <p className="eyebrow">OPC UA Studio</p>
      <h1>Web workspace ready</h1>
      <p>
        React is running in the browser. OPC UA connections will be owned by the
        Node.js server through <code>node-opcua</code>.
      </p>
    </main>
  );
}
